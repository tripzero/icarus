Notes
tripzero> the library is called pysolar

<tripzero> step 1) use pysolar to get solar elevation
<tripzero> step 2) do math to figure out how high the linear actuator needs to be to be 45 degrees based on elevation
<tripzero> step 3) use mraa to move the linear actuator up and down.

ina219= a current sensor that we won't be using ;)
<tripzero> i'll be updating that code to use a different sensor



SWIG:
-
DSWIG_EXECUTABLE=/usr/bin/swig

Goal:can get the solar elevation and output the optimal height of the actuator, we'll be almost there
................................
def getsolarheading():
        Azimuth = Pysolar.GetAzimuth(maplat, maplon,
datetime.datetime.utcnow())
        if Azimuth < 0:
                if (Azimuth >= -180):
                        solarheading = ((Azimuth * -1) + 180)
                if (Azimuth < -180):
                        solarheading = ((Azimuth * -1) - 180)
        if Azimuth >= 0:
                solarheading = Azimuth
        return solarheading
...................................